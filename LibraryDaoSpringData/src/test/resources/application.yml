spring:
    application:
        name: library-service
    jpa:
        database: h2
        show-sql: true
    jackson:
        serialization:
            write-dates-as-timestamps: false
    cache:
        type: jcache
        cache-names: users, books
flyway:
    enabled: false
borrow:
    maximum: 2

