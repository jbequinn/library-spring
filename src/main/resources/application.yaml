spring:
  application:
    name: library-service
  datasource:
    url: jdbc:postgresql://172.17.0.2:5432/librarydb
    username: libraryuser
    password: librarypassword
    initialize: true
  jpa:
    properties:
      hibernate:
        default_schema: library
  flyway:
    schemas: library
borrow:
  maximum: 3
  borrowLength: 2
